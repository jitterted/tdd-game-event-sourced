Next up:
========

Joining Game
------------

[ ] Create JoinGame command
    [ ] Just generate PlayerJoined event
        [ ] Rule: game must have space available for a new player
        [ ] Rule: member must not already be joined in that game
    [ ] Call JoinGame from the LobbyController.joinGame()
    [X] Reuse the Username-to->Member query in LobbyController (put it in Members.findIdFor(Username), where Members references the EventStore?)
        [X] Get member ID mapped from Principal.name via a Decision Query (which becomes part of the conditional append query)
[ ] Redirect from joinGame to /game with the game handle as a query param

[ ] When showing games available to Join, mark those that the member already joined so they don't try to join them again

CLEAN UP
========

[ ] Generate MemberId upon registration (execution of the command after the POST), instead of preloading the form with a MemberId.


DEFERRED
========
[ ] MVC-level test that does GET "/lobby"->Follow Redirect->POST "/register"->GET "/lobby" ensuring that the sequence works properly where the final GET returns the "lobby" view name


Completed:
==========

[X] Add security principal username to the GameCreated event
[X] EventStore impl has to assign event sequence numbers to events
[X] Add game handle to projector for Slice #2 UI
[X] JoinGameController populates template with available games

Member Registration
-------------------
[X] After login we redirect to Lobby: /lobby needs to ensure you're registered as a Member, and if not, redirects to /register
[X] /register does RegisterMemberCommand, which (if success) results in MemberRegistered
    [X] Controller GET endpoint for "/register" that returns a Form with a new MemberId
        [X] Thymeleaf template
    [X] Register the new member
        [X] Controller POST for "/register"
        [X] Execute Register Member Command with MemberId from the Form and username from Principal.getName()
        [X] Redirect to /lobby
